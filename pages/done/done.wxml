<view class="container">
    <view class="lists">
        <view class="nodata" wx:if="{{completed.length<1}}">暂无数据</view>
        <component-todo bindstate="onStateChange" todo="{{item}}" wx:for="{{completed}}" wx:key="{{item.id}}"></component-todo>
    </view>
</view>
